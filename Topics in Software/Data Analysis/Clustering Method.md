# K-Means Clustering
- *Definition:* group points into K groups based on **Euclidean distance** 
# Region Growing Segmentation
# Min-Cut (Graph-Based) Segmentation
# Euclidean Clustering
- *Definition:* If two points are close together (based on **Euclidean distance**) then they belong to a cluster. If the distance exceed a threshold, then the point belongs to another cluster